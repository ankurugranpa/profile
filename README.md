# Social Profile

Facebook・X・Gmailへのリンクをまとめた、モバイルファーストのプロフィールサイトです。
React、TypeScript、Tailwind CSS、shadcn/uiで構成しています。

## 開発

```bash
npm install
npm run dev
```

## 内容の変更

`src/data/profile.ts` にプロフィールとリンクの設定を集約しています。
表示内容を変更する場合は、このファイルを編集してください。
